# Plan #2

Intent as read: falsify three claims about countersign-system's architecture schema, then record the
result; if the Operator wants any claim made true, that is a later plan.

Mutation: one falsification record, `agent-corpus/falsification/zones-schema.md` (this ledger's agent
zone), holding the attacks A1–A6 below and their results; nothing else. No schema, guard or
dependency change.

Files touched: agent-corpus/falsification/zones-schema.md.

Base commit: see the row's first commit.

Falsification (Rule-9):
| # | claim | attack | result |
|---|---|---|---|
| A1 | `zones` is a type in the architecture schema | read the schema's `$defs` (`crafts/countersign/templates/countersign-core.json`) | Refuted: 14 defs (id, ref, optref, extref, profile, party, act, proposal, countersignature, mandate, release, event, escalation, check); no zone. `rules/schema.md` F5 classes zone as *profile only*, an extension outside the core |
| A2 | …or anywhere as a type | where zones do exist | Refuted as "type": `dyad/guards/infra/containment.py` `ZONES`, a list of `(zone, glob)` tuples plus `ZONE_NAMES`, owned by the core craft's Rule-1, not by the Countersign schema |
| A3 | enum is {agent, craft, preferences, infra} | print the installed table (`containment.py zones`) | Refuted: `ZONE_NAMES` has five — `workstation` too (`workstation-corpus/*`), inherited from dyad-operator 0.9.1 |
| A4 | …scoped to what countersign-system uses | list its tracked roots | Survives, scoped: no `workstation-corpus/` exists, so only those four zones are inhabited; the fifth is declared and empty |
| A5 | the schema is defined via pydantic | read the schema and its validator | Refuted: JSON Schema draft 2020-12, hand-written; `project_countersign.py` says "stdlib only (no pydantic, no jsonschema)" |
| A6 | …or pydantic is at least in play | manifest and code | Refuted as fact, survives as intent: pydantic 2.13.5 is a kernel manifest row "not yet installed — adopted when a plan first imports it"; no `BaseModel` anywhere in `dyad/` or `crafts/` |

## Revision 2 (2026-09-25, after `N: re-scope #2 … host infrastructure first`)

Intent as read: before any schema work, define the host infrastructure countersign-system itself runs
on — the LLM agent (Claude Code), Git, Python, pydantic — as countersign-system's own record, because
the Operator holds that without it countersign cannot start operating. Revision 1's zones/schema
record (A1–A6 above) is kept as findings and is folded into this record, not dropped.

Falsification of the new claim ("without a host-infrastructure definition we can't start operating
countersign"):

| # | attack | result |
|---|---|---|
| B1 | does countersign-system operate today without its own definition? | Refuted as stated: row #1 ran end to end, the guards and `dyad check --evidence` are green (416cb17), hooks run on push, row #2 is ledgered. It operates on the core craft's manifest and the `dyad-python` resolver |
| B2 | is the definition it runs on its own? | Survives: `dyad/infrastructure/INFRASTRUCTURE.md` is the core craft's file, copied verbatim; its kernel rows were observed on dyad-system's host 2026-09-13 (Claude Code 2.1.270, Python 3.12.3), and its library rows are dyad-system's instance (Gitea, lan-git, Docker, GHCR, gh, jq). Nothing records this host |
| B3 | does the frame point at a host record? | Survives, sharper: `dyad/CLAUDE.md` says "Host: see `workstation-corpus/`" and "Facts about the host live in `workstation-corpus/`" — countersign-system has no `workstation-corpus/`. The reference dangles; the `workstation` zone (A3) is the place the frame already names |
| B4 | is every kernel component present here? | Refuted for pydantic: declared `kernel` and pinned 2.13.5, but not importable by the resolved interpreter (`/usr/bin/python3.13` 3.13.12). Claude Code observed 2.1.282, Git 2.43.0, default `python3` 3.11.15 (below the pin; the resolver skips it) |
| B5 | is "can't start" true for pydantic specifically? | Survives, scoped: nothing imports pydantic today, so operation is unblocked; but any pydantic-defined schema (revision 1's counter) cannot run here until it is installed |
| B6 | is this host durable enough to define once? | Survives as a risk: this is an ephemeral cloud container; an install done here is lost on reclaim. The durable form is the environment's setup script (Operator-side) plus a record of what it must provide |

Mutation (countersign-system, one d-work, zone-split per Rule-1):
- PR-A (workstation zone): `workstation-corpus/HOST.md` — this host's kernel as observed (Claude Code,
  Python pin and the interpreter the resolver picks, Git, pydantic required/absent), the runtime (cloud
  container, ephemeral), and what a session must find present; closes the frame's dangling reference.
  `workstation-corpus/ops/2-h1-kernel-setup.sh` — the Rule-18 ops script that installs pydantic 2.13.5
  into the resolved interpreter and verifies all four kernel components (read-only checks + one
  reversible install; undo `pip uninstall pydantic`), for the Operator to run or to paste into the
  environment's setup script.
- PR-B (agent zone): `agent-corpus/falsification/host-infrastructure.md` — A1–A6 and B1–B6.
- Not in scope: editing the core craft's manifest (a core change, dyad-system's to make; B2 is reported
  there as a finding), installing anything myself, adopting pydantic in any schema.

Host action: none by the Agent. The ops script is delivered for the Operator (Rule-18).

Files touched: workstation-corpus/HOST.md, workstation-corpus/ops/2-h1-kernel-setup.sh,
agent-corpus/falsification/host-infrastructure.md.

## Revision 3 (2026-09-25, after dyad-system #175 landed: core 0.10.0, host path/zone, manifest profiles)

Intent as read: give countersign-system its own operating infrastructure record under `infrastructure/`
(zone `infra`, four zones), including an executable Python environment (venv definition) for the
kernel, so it can start operating without borrowing dyad-system's host facts. Revisions 1–2's
findings (A1–A6, B1–B6) and the authoring/operating synthesis (dyad-system #175, S1–S6, P1–P6) hold.

Mutation (countersign-system; one zone per PR, referent before referrer):
1. agent — reinstall the core from dyad-system `main` (dyad-operator 0.10.0): `dyad install`.
2. craft — reinstall sysadmin 0.2.0 (bundled) and upgrade the Tended crafts the core now requires;
   countersign stays 0.2.0 (unchanged at source).
3. preferences — add `host-path: infrastructure` and `host-zone: infra` (+ the eight existing values).
4. infra — `infrastructure/HOST.md` (this host as observed: Claude Code, Git, Python pin and resolved
   interpreter, pydantic, the ephemeral cloud container and its setup script);
   `infrastructure/INFRASTRUCTURE.md` (this system's operating rows, profile `operating`: the venv);
   `infrastructure/python/requirements.txt` (`pydantic==2.13.5`, equal to the kernel pin);
   `infrastructure/ops/2-h1-kernel-setup.sh` (Rule-18 form: builds `.venv/` with the resolved
   interpreter, installs the requirements, verifies the four kernel components; undo: remove `.venv/`);
   `.gitignore` gains `.venv/`.
5. agent — `agent-corpus/falsification/host-infrastructure.md`: A1–A6, B1–B6, V1–V5, and the pointer
   to dyad-system #175's record for S/P.

Host actions by the Agent: none (the setup script is delivered for the Operator, or pasted into the
environment's setup script). Evidence: `dyad check --evidence` green on the top of the stack, zone
set {agent, preferences, infra, craft}, the instance manifest loaded.

Files touched: dyad/**, crafts/**, preferences-corpus/PREFERENCES.md, infrastructure/**, .gitignore,
agent-corpus/falsification/host-infrastructure.md.

Base commit (revision 3): see the ledger commit that adds this revision.
